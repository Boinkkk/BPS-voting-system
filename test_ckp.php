<?php require 'vendor/autoload.php'; $app = require_once 'bootstrap/app.php'; $app->make(Illuminate\Contracts\Console\Kernel::class)->bootstrap(); $periodeId = 1; $pegawais = \App\Models\Pegawai::whereHas('role', function($q) { $q->where('tipe', 'Pegawai'); })->get(); foreach ($pegawais as $pegawai) { $ckp = \App\Models\NilaiCkp::where('periode_id', $periodeId)->where('pegawai_id', $pegawai->id)->first(); dump(['id' => $pegawai->id, 'ckp' => $ckp ? $ckp->nilai : 0]); }
